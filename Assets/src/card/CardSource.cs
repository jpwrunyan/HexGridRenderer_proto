/// <summary>
/// Card source is meant to hold the stats to determine a card's specific action values?
/// This design needs work.
/// </summary>
public interface CardSource {
    //CardMap + CardSource = Card w/ actions
}
